[pubmsg]
format = %(target)s: <%(source)s> %(message)s

[pubaction]
format = %(target)s: * %(source)s %(message)s

[privaction]
format = * %(source)s %(message)s

[privmsg]
format = <%(source)s> %(message)s
